name: Daily Data Update

on:
  schedule:
    # Runs at 8:00 PM IST (2:30 PM UTC) every day
    # IST = UTC + 5:30, so 8:00 PM IST = 14:30 UTC
    # 8 PM chosen because: markets close at 3:30 PM IST, mutual fund NAVs publish by 6-7 PM,
    # and Yahoo Finance data settles by ~7:30 PM IST. Gives users fresh data for evening review.
    - cron: '30 14 * * *'
  workflow_dispatch: # allows manual trigger from GitHub website

permissions:
  contents: write
  issues: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests pdfplumber beautifulsoup4 yfinance

      - name: Run weather scraper
        run: python scraper/runner.py

      - name: Run sector returns scraper (Yahoo Finance)
        run: python scraper/sector_returns_scraper.py

      - name: Run stock fundamentals scraper (Yahoo Finance)
        run: python scraper/stock_fundamentals_scraper.py

      - name: Run analysis engine
        run: python scraper/analysis_runner.py

      - name: Run rainfall-stock correlation
        run: python scraper/rainfall_stock_correlation.py

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/latest.json data/history.json docs/latest.json docs/history.json docs/analysis.json docs/sector_returns.json docs/stock_fundamentals.json docs/rainfall_correlation.json
          git diff --cached --quiet || git commit -m "Daily data update $(date -u +%Y-%m-%d)"
          git push

      - name: Close stale failure issue if run succeeded
        if: success()
        run: |
          ISSUE=$(gh issue list --label "bot:data-update-failed" --state open --json number -q '.[0].number')
          if [ -n "$ISSUE" ]; then
            gh issue close "$ISSUE" -c "Resolved: daily update succeeded on $(date -u +%Y-%m-%d)."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Open issue on failure
        if: failure()
        run: |
          EXISTING=$(gh issue list --label "bot:data-update-failed" --state open --json number -q '.[0].number')
          if [ -n "$EXISTING" ]; then
            gh issue comment "$EXISTING" --body "Still failing as of $(date -u +%Y-%m-%d %H:%M) UTC. [Run log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          else
            gh issue create \
              --title "Daily data update failed $(date -u +%Y-%m-%d)" \
              --label "bot:data-update-failed" \
              --body "The daily scraper workflow failed. [View run log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). This issue will auto-close when the next run succeeds."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
